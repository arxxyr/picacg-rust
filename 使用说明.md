# PicACG Rust 版本 - 使用说明

## 功能概览

已实现的功能：
- ✅ 用户登录
- ✅ 分类浏览
- ✅ 漫画列表（分页）
- ✅ 漫画详情页
- ✅ 章节列表（分页）
- ✅ 阅读界面
- ✅ 图片浏览（翻页、缩放）
- ✅ 点赞和收藏
- ✅ 代理设置

## 运行程序

### 开发模式
```powershell
cd picacg-rust
cargo run
```

### 发布模式
```powershell
cd picacg-rust
cargo run --release
```

### 带调试信息运行
```powershell
cd picacg-rust
$env:RUST_BACKTRACE = "1"
cargo run
```

## 使用流程

### 1. 登录

1. 启动程序后会显示登录界面
2. 输入用户名和密码
3. 可选：点击"代理设置"配置代理
4. 点击"登录"或按 Enter 键

**快捷键：**
- `Tab`: 切换输入焦点
- `Enter`: 登录

### 2. 浏览分类

1. 登录成功后，点击左侧菜单的"分类"
2. 程序会自动加载所有分类
3. 点击任意分类进入该分类的漫画列表

### 3. 浏览漫画列表

1. 漫画列表会显示该分类下的所有漫画
2. 支持分页浏览（点击"上一页"/"下一页"）
3. 点击任意漫画进入详情页

### 4. 查看漫画详情

漫画详情页显示：
- 封面图片
- 标题、作者、汉化组
- 浏览数、点赞数、评论数
- 完结状态、章节数、页数
- 分类标签
- 简介

**操作按钮：**
- **开始阅读**: 跳转到第一章开始阅读
- **收藏**: 收藏/取消收藏该漫画
- **点赞**: 点赞/取消点赞该漫画
- **加载章节列表**: 加载该漫画的所有章节

### 5. 加载章节列表

1. 点击"加载章节列表"按钮
2. 章节会以网格形式显示（每行 4 个）
3. 支持分页浏览（如果章节很多）
4. 点击任意章节进入阅读界面

### 6. 阅读界面

阅读界面包含三个区域：

**顶部控制栏：**
- 返回：返回详情页
- 页码信息：显示当前章节和页码
- 模式切换：切换阅读模式（暂未实现）

**图片显示区域：**
- 显示当前页的图片
- 支持缩放查看

**底部控制栏：**
- 上一章/下一章：切换章节
- ◀/▶ 按钮：翻页
- -/+ 按钮：缩放控制
- 重置：重置缩放比例
- 缩放百分比显示

**翻页行为：**
- 当前页 < 总页数：翻到下一页
- 已到最后一页：自动跳转到下一章
- 当前页 > 1：翻到上一页
- 已到第一页且第一章：无操作

## 快捷键

### 登录界面
- `Tab`: 切换焦点（用户名 → 密码 → 登录按钮 → 代理设置 → 用户名）
- `Enter`:
  - 在输入框或登录按钮：执行登录
  - 在代理设置按钮：跳转到代理设置

### 阅读界面
- 返回：Esc（待实现）
- 上一页：← / PageUp（待实现）
- 下一页：→ / PageDown（待实现）
- 放大：+ / =（待实现）
- 缩小：-（待实现）
- 全屏：F11（待实现）

## 注意事项

### 1. 网络连接

- 如果无法访问 API，请配置代理
- 代理支持 HTTP、HTTPS、SOCKS5 协议
- 可以在登录前或设置页面配置代理

### 2. 图片加载

- 图片会自动缓存，第二次访问更快
- 如果图片加载失败，会显示错误信息
- 可以点击"重试"按钮重新加载

### 3. 章节加载

- 章节列表需要手动点击"加载章节列表"
- 这是为了避免自动加载导致的额外流量
- 章节列表支持分页（每页显示多个章节）

### 4. 阅读体验

- 首次加载图片可能较慢，请耐心等待
- 图片会缓存到内存，翻页会更流畅
- 缩放功能支持 50% - 300% 范围

## 常见问题

### Q: 登录失败怎么办？

A: 检查以下几点：
1. 用户名和密码是否正确
2. 网络连接是否正常
3. 是否需要配置代理
4. 查看错误提示信息

### Q: 图片加载很慢怎么办？

A:
1. 检查网络连接速度
2. 如果在国内，建议配置代理
3. 图片首次加载会较慢，之后会缓存

### Q: 如何返回上一页？

A:
- 详情页：点击左侧菜单或浏览器后退按钮
- 阅读界面：点击顶部的"返回"按钮

### Q: 如何切换章节？

A:
- 方法1：点击底部的"上一章"/"下一章"按钮
- 方法2：返回详情页，点击其他章节

### Q: 点赞和收藏后如何查看？

A:
- 点赞和收藏会立即生效
- 按钮文字会变为"已点赞"/"已收藏"
- 刷新详情页可以看到最新状态

## 开发信息

### 技术栈
- Rust (nightly)
- iced 0.13 (GUI 框架)
- reqwest (HTTP 客户端)
- serde (序列化)

### 项目结构
```
picacg-rust/
├── src/
│   ├── api/          # API 客户端和接口定义
│   ├── ui/           # UI 视图和状态管理
│   ├── db/           # 数据库（未使用）
│   ├── download/     # 下载管理（未使用）
│   ├── config/       # 配置管理
│   └── main.rs       # 程序入口
├── resources/        # 资源文件（字体等）
└── Cargo.toml        # 项目配置
```

### 编译优化

发布版本会启用以下优化：
- LTO (Link Time Optimization)
- 代码大小优化
- 去除调试信息
- 原生 CPU 指令集

### 性能提示

- 首次启动会较慢（加载字体等资源）
- 图片缓存在内存中，占用会逐渐增加
- 建议定期重启程序释放内存

## 下一步计划

### 待实现功能
- [ ] 搜索功能
- [ ] 收藏列表
- [ ] 下载管理
- [ ] 历史记录
- [ ] 图片预加载
- [ ] 键盘快捷键
- [ ] 双页阅读模式
- [ ] 连续滚动模式
- [ ] Waifu2x 图片超分

### 性能优化
- [ ] 图片懒加载
- [ ] 虚拟滚动
- [ ] 内存管理优化
- [ ] 异步图片解码

## 报告问题

如果遇到问题，请提供以下信息：
1. 错误提示信息
2. 操作步骤
3. 系统版本（Windows/Linux/macOS）
4. 是否使用代理
5. 日志输出（如果有）

运行时可以通过设置环境变量查看详细日志：
```powershell
$env:RUST_LOG = "debug"
cargo run
```
